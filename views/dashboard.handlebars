<h1 id='dashboard-welcome'>Welcome, {{username}}</h1>

{{#each blogs as |blog|}}
{{> blog-details}}
{{/each}}
<div class='container-fluid mt-5 d-grid gap-2 d-md-flex justify-content-center'>
    <a class='btn btn-primary custom-btn' href=' /new-post'>New Post</a>
</div>

<script src='../js/logout.js'></script>